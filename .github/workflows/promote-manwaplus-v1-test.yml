name: Promote Manwa Plus v1.6.4 test

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/promote-manwaplus-v1-test.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: promote-manwaplus-test
  cancel-in-progress: false

jobs:
  promote:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - name: Checkout public repo branch
        uses: actions/checkout@v5
        with:
          ref: repo
          fetch-depth: 0
          persist-credentials: true

      - name: Fetch pinned staged Manwa Plus v1.6.4
        shell: bash
        run: |
          set -euo pipefail
          staging_sha='33ca09e0993cf39e62f58a1a41c6212c1d40cce7'
          base="https://raw.githubusercontent.com/huoguotiankong/mx-build/$staging_sha/test"
          curl -fsSL --retry 3 "$base/index.json" -o /tmp/staged-index.json
          curl -fsSL --retry 3 "$base/apk/tachiyomi-zh.manwaplus-v1.6.4.apk" -o /tmp/manwaplus.apk
          curl -fsSL --retry 3 "$base/icon/eu.kanade.tachiyomi.extension.zh.manwaplus-v106004.png" -o /tmp/manwaplus.png
          test -s /tmp/manwaplus.apk
          test -s /tmp/manwaplus.png
          sha256sum /tmp/manwaplus.apk | tee /tmp/manwaplus.sha256
          python3 - <<'PY'
          import json
          from pathlib import Path

          package = 'eu.kanade.tachiyomi.extension.zh.manwaplus'
          staged = json.loads(Path('/tmp/staged-index.json').read_text('utf-8'))
          items = [x for x in staged['extensionList']['extensions'] if x.get('packageName') == package]
          if len(items) != 1:
              raise SystemExit(f'Expected one staged Manwa Plus item, got {len(items)}')
          item = items[0]
          if (item.get('versionName'), item.get('versionCode')) != ('1.6.4', 106004):
              raise SystemExit(f'Unexpected staged Manwa Plus version: {(item.get("versionName"), item.get("versionCode"))}')
          if item.get('extensionLib') != '1.6':
              raise SystemExit(f'Unexpected extensionLib: {item.get("extensionLib")}')
          sources = item.get('sources') or []
          if len(sources) != 1 or sources[0].get('name') != '漫蛙 Plus':
              raise SystemExit('Unexpected staged Manwa Plus source metadata')
          if sources[0].get('homeUrl') != 'https://manwa.me':
              raise SystemExit(f'Unexpected Manwa Plus homeUrl: {sources[0].get("homeUrl")}')
          Path('/tmp/manwaplus-entry.json').write_text(
              json.dumps(item, ensure_ascii=False),
              encoding='utf-8',
          )
          PY

      - name: Promote only Manwa Plus v1.6.4 to test store
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json
          from pathlib import Path

          package = 'eu.kanade.tachiyomi.extension.zh.manwaplus'
          public_path = Path('test/index.json')
          public = json.loads(public_path.read_text('utf-8'))
          staged_item = json.loads(Path('/tmp/manwaplus-entry.json').read_text('utf-8'))
          items = public['extensionList']['extensions']
          positions = [i for i, x in enumerate(items) if x.get('packageName') == package]
          if len(positions) > 1:
              raise SystemExit(f'Expected at most one public Manwa Plus item, got {len(positions)}')
          if positions:
              items[positions[0]] = staged_item
          else:
              items.append(staged_item)
          items.sort(key=lambda x: (str(x.get('name', '')).casefold(), str(x.get('packageName', ''))))
          public_path.write_text(json.dumps(public, ensure_ascii=False, indent=2) + '\n', encoding='utf-8')
          PY
          rm -f test/apk/tachiyomi-zh.manwaplus-v*.apk
          rm -f test/icon/eu.kanade.tachiyomi.extension.zh.manwaplus-v*.png
          cp /tmp/manwaplus.apk test/apk/tachiyomi-zh.manwaplus-v1.6.4.apk
          cp /tmp/manwaplus.png test/icon/eu.kanade.tachiyomi.extension.zh.manwaplus-v106004.png
          git status --short

      - name: Commit and push Manwa Plus v1.6.4 test
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add test/index.json test/apk test/icon
          git diff --cached --check
          if git diff --cached --quiet; then
            echo 'No Manwa Plus test-store changes to publish'
          else
            git commit -m 'Publish Manwa Plus v1.6.4 test'
            git push origin HEAD:repo
          fi

      - name: Verify published files locally
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import hashlib
          import json
          from pathlib import Path

          package = 'eu.kanade.tachiyomi.extension.zh.manwaplus'
          data = json.loads(Path('test/index.json').read_text('utf-8'))
          items = [x for x in data['extensionList']['extensions'] if x.get('packageName') == package]
          assert len(items) == 1
          item = items[0]
          assert (item.get('versionName'), item.get('versionCode')) == ('1.6.4', 106004)
          assert item.get('extensionLib') == '1.6'
          source = (item.get('sources') or [{}])[0]
          assert source.get('name') == '漫蛙 Plus'
          assert source.get('homeUrl') == 'https://manwa.me'
          apk = Path('test/apk/tachiyomi-zh.manwaplus-v1.6.4.apk')
          icon = Path('test/icon/eu.kanade.tachiyomi.extension.zh.manwaplus-v106004.png')
          assert apk.is_file() and icon.is_file()
          staged_hash = Path('/tmp/manwaplus.sha256').read_text().split()[0]
          assert hashlib.sha256(apk.read_bytes()).hexdigest() == staged_hash
          print('Manwa Plus v1.6.4 local test promotion verified:', staged_hash)
          PY
