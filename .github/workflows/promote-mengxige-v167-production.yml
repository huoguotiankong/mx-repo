name: Promote MengXiGe v1.6.7 production

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/promote-mengxige-v167-production.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: promote-mengxige-production
  cancel-in-progress: false

jobs:
  promote:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: Checkout formal repository
        uses: actions/checkout@v5
        with:
          ref: repo
          path: repo
          fetch-depth: 0
          persist-credentials: true

      - name: Checkout validated MengXiGe staging
        uses: actions/checkout@v5
        with:
          repository: huoguotiankong/mx-build
          ref: formal-staging
          path: staging
          fetch-depth: 1
          persist-credentials: false

      - name: Verify staged MengXiGe v1.6.7 and concurrent-release guard
        shell: bash
        run: |
          set -euo pipefail
          h='staging/formal'
          test -f "$h/VALIDATED.txt"
          test -f "$h/SOURCE_SHA.txt"
          grep -Fx 'module=mengxige' "$h/VALIDATED.txt"
          grep -Fx 'version=1.6.7' "$h/VALIDATED.txt"
          grep -Fx 'canonicalVersionCode=106007' "$h/VALIDATED.txt"
          grep -Fx 'androidVersionCode=212007' "$h/VALIDATED.txt"
          test "$(tr -d '\r\n' < "$h/SOURCE_SHA.txt")" = '15560923a442cde80b8a8a78e35ee2dee9fd2ac9'
          test -f "$h/tachiyomi-zh.mengxige-v1.6.7.apk"
          test -f "$h/tachiyomi-zh.mengxige-v1.6.7.jar"
          test -f "$h/icon/eu.kanade.tachiyomi.extension.zh.mengxige-v106007.png"
          test "$(stat -c%s "$h/icon/eu.kanade.tachiyomi.extension.zh.mengxige-v106007.png")" -gt 1000
          for file in index.json index.min.json index.pb index-android.json index-android.min.json index-android.pb published-state.json repo.json; do
            test -f "$h/$file"
          done
          python3 - <<'PY'
          import json
          from pathlib import Path
          package='eu.kanade.tachiyomi.extension.zh.mengxige'
          h=Path('staging/formal')
          def load(path): return json.loads(path.read_text('utf-8'))
          handoff=load(h/'index.json')
          current=load(Path('repo/index.json'))
          hext={x['packageName']:x for x in handoff['extensionList']['extensions']}
          cext={x['packageName']:x for x in current['extensionList']['extensions']}
          assert package in hext
          m=hext[package]
          assert m['versionName']=='1.6.7' and int(m['versionCode'])==106007, m
          assert m['resources']['apkUrl'].endswith('/tachiyomi-zh.mengxige-v1.6.7.apk'), m
          assert m['resources']['jarUrl'].endswith('/tachiyomi-zh.mengxige-v1.6.7.jar'), m
          assert m['resources']['iconUrl'].endswith('/icon/eu.kanade.tachiyomi.extension.zh.mengxige-v106007.png'), m
          hs=dict(hext); hs.pop(package, None)
          cs=dict(cext); cs.pop(package, None)
          if hs != cs:
              raise SystemExit('Formal repository changed after staging generation; refusing to overwrite another release')
          android=load(h/'index-android.json')
          am=[x for x in android['extensionList']['extensions'] if x.get('packageName')==package]
          assert len(am)==1 and int(am[0]['versionCode'])==212007, am
          hstate=load(h/'published-state.json')
          cstate=load(Path('repo/published-state.json'))
          assert hstate[package]['versionName']=='1.6.7', hstate[package]
          assert int(hstate[package]['versionCode'])==106007, hstate[package]
          assert int(hstate[package]['androidVersionCode'])==212007, hstate[package]
          hs_state=dict(hstate); hs_state.pop(package, None)
          cs_state=dict(cstate); cs_state.pop(package, None)
          if hs_state != cs_state:
              raise SystemExit('Published state changed after staging generation; refusing to overwrite another release')
          icon=h/'icon/eu.kanade.tachiyomi.extension.zh.mengxige-v106007.png'
          assert icon.read_bytes()[:8] == b'\x89PNG\r\n\x1a\n'
          print('MengXiGe v1.6.7 staging and concurrent-release guard verified')
          PY

      - name: Apply formal MengXiGe v1.6.7 handoff
        shell: bash
        run: |
          set -euo pipefail
          h='staging/formal'
          rm -f repo/tachiyomi-zh.mengxige-v*.apk repo/tachiyomi-zh.mengxige-v*.jar
          rm -f repo/icon/eu.kanade.tachiyomi.extension.zh.mengxige-v*.png
          cp "$h/tachiyomi-zh.mengxige-v1.6.7.apk" repo/
          cp "$h/tachiyomi-zh.mengxige-v1.6.7.jar" repo/
          cp "$h/icon/eu.kanade.tachiyomi.extension.zh.mengxige-v106007.png" repo/icon/
          for file in index.json index.min.json index.pb index-android.json index-android.min.json index-android.pb published-state.json repo.json; do
            cp "$h/$file" "repo/$file"
          done

      - name: Verify formal repository before push
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json
          from pathlib import Path
          package='eu.kanade.tachiyomi.extension.zh.mengxige'
          normal=json.loads(Path('repo/index.json').read_text('utf-8'))
          m=[x for x in normal['extensionList']['extensions'] if x.get('packageName')==package]
          assert len(m)==1 and m[0]['versionName']=='1.6.7' and int(m[0]['versionCode'])==106007, m
          android=json.loads(Path('repo/index-android.json').read_text('utf-8'))
          a=[x for x in android['extensionList']['extensions'] if x.get('packageName')==package]
          assert len(a)==1 and int(a[0]['versionCode'])==212007, a
          state=json.loads(Path('repo/published-state.json').read_text('utf-8'))
          assert state[package]['versionName']=='1.6.7', state[package]
          icon=Path('repo/icon/eu.kanade.tachiyomi.extension.zh.mengxige-v106007.png')
          assert icon.is_file() and icon.stat().st_size > 1000
          assert icon.read_bytes()[:8] == b'\x89PNG\r\n\x1a\n'
          assert Path('repo/index.pb').is_file()
          assert Path('repo/tachiyomi-zh.mengxige-v1.6.7.apk').is_file()
          assert Path('repo/tachiyomi-zh.mengxige-v1.6.7.jar').is_file()
          print('Formal MengXiGe v1.6.7 snapshot verified before push')
          PY
          git -C repo add -A
          git -C repo diff --cached --check

      - name: Commit and push formal plugin store
        working-directory: repo
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          if git diff --cached --quiet; then
            echo 'MengXiGe v1.6.7 is already published'
          else
            git commit -m 'Publish MengXiGe v1.6.7'
            git pull --rebase origin repo
            git push origin HEAD:repo
          fi

      - name: Verify pushed formal plugin store
        shell: bash
        run: |
          set -euo pipefail
          git -C repo fetch origin repo
          git -C repo reset --hard origin/repo
          python3 - <<'PY'
          import json
          from pathlib import Path
          package='eu.kanade.tachiyomi.extension.zh.mengxige'
          normal=json.loads(Path('repo/index.json').read_text('utf-8'))
          m=[x for x in normal['extensionList']['extensions'] if x.get('packageName')==package]
          assert len(m)==1 and m[0]['versionName']=='1.6.7' and int(m[0]['versionCode'])==106007, m
          android=json.loads(Path('repo/index-android.json').read_text('utf-8'))
          a=[x for x in android['extensionList']['extensions'] if x.get('packageName')==package]
          assert len(a)==1 and int(a[0]['versionCode'])==212007, a
          icon=Path('repo/icon/eu.kanade.tachiyomi.extension.zh.mengxige-v106007.png')
          assert icon.is_file() and icon.stat().st_size > 1000
          assert Path('repo/index.pb').is_file()
          assert Path('repo/tachiyomi-zh.mengxige-v1.6.7.apk').is_file()
          assert Path('repo/tachiyomi-zh.mengxige-v1.6.7.jar').is_file()
          print('Formal MengXiGe v1.6.7 plugin-store push verified')
          PY
